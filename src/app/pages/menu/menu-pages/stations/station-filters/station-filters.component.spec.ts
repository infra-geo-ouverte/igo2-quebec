/*
import { ComponentFixture, TestBed } from '@angular/core/testing';

import { StationFiltersComponent } from './station-filters.component';

describe('StationFiltersComponent', () => {
  let component: StationFiltersComponent;
  let fixture: ComponentFixture<StationFiltersComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ StationFiltersComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(StationFiltersComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
