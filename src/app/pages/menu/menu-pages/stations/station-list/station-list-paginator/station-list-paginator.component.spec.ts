/*
import { ComponentFixture, TestBed } from '@angular/core/testing';

import { StationListPaginatorComponent } from './station-list-paginator.component';

describe('StationListPaginatorComponent', () => {
  let component: StationListPaginatorComponent;
  let fixture: ComponentFixture<StationListPaginatorComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ StationListPaginatorComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(StationListPaginatorComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
