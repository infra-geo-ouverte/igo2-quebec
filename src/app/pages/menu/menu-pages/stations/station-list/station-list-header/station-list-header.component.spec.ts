/*
import { ComponentFixture, TestBed } from '@angular/core/testing';

import { StationListHeaderComponent } from './station-list-header.component';

describe('StationListHeaderComponent', () => {
  let component: StationListHeaderComponent;
  let fixture: ComponentFixture<StationListHeaderComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ StationListHeaderComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(StationListHeaderComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
