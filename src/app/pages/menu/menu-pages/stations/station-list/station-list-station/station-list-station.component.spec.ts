/*
import { ComponentFixture, TestBed } from '@angular/core/testing';

import { StationListStationComponent } from './station-list-station.component';

describe('StationListStationComponent', () => {
  let component: StationListStationComponent;
  let fixture: ComponentFixture<StationListStationComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ StationListStationComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(StationListStationComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
