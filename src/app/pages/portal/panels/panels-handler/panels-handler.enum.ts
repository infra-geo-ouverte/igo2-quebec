export enum ShownComponent {
  Query = 'query',
  Search = 'search',
  Legend = 'legend',
  Filter = 'filter'
}

export enum SearchResultAction {
  Focus = 'focus',
  Select = 'select',
  Unfocus = 'unfocus'
}
